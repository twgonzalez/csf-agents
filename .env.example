# =============================================================================
# California Stewardship Fund — Credentials Template
# =============================================================================
# Copy this file to .env and fill in your values.
# .env is listed in .gitignore and will NEVER be committed.
# =============================================================================

# ---------------------------------------------------------------------------
# Anthropic API key (required by housing_analyzer agent)
# Get a free key at https://console.anthropic.com/
# ---------------------------------------------------------------------------
ANTHROPIC_API_KEY=

# ---------------------------------------------------------------------------
# LegiScan dataset auto-download
# Creates a free account at https://legiscan.com/legiscan (instant signup).
# With these set, the tracker will automatically download the latest CA
# dataset ZIP each week instead of requiring a manual download.
# ---------------------------------------------------------------------------
LEGISCAN_USER=
LEGISCAN_PASSWORD=

# ---------------------------------------------------------------------------
# LegiScan API key (optional — requires human review, 1-2 business days)
# Unlocks real-time data instead of the weekly ZIP.
# Free tier: 30,000 queries/month.
# ---------------------------------------------------------------------------
LEGISCAN_API_KEY=

# ---------------------------------------------------------------------------
# OpenStates API key (optional — requires human review, 1-2 business days)
# Alternative real-time source if LegiScan API is unavailable.
# Free tier: 500 queries/day.
# ---------------------------------------------------------------------------
OPENSTATES_API_KEY=

# ---------------------------------------------------------------------------
# Email delivery (optional)
# Gmail setup: myaccount.google.com → Security → App passwords → generate one
# EMAIL_RECIPIENTS overrides the list in config.yaml — comma-separated.
# Example: EMAIL_RECIPIENTS=alice@example.com,bob@example.com
# ---------------------------------------------------------------------------
EMAIL_USER=
EMAIL_PASSWORD=
EMAIL_RECIPIENTS=

# ---------------------------------------------------------------------------
# NewsAPI key (optional — used by media_scanner agent)
# Free tier (100 req/day, 1-month lookback) is sufficient for weekly runs.
# Get a free key at: https://newsapi.org/register
# Without this key, media_scanner falls back to RSS feeds only.
# ---------------------------------------------------------------------------
NEWSAPI_KEY=

# ---------------------------------------------------------------------------
# X (Twitter) API Bearer Token (optional — stub in media_scanner agent)
# Required for social listening (X post search) via media_scanner.py.
# Requires: X Developer account + Basic tier ($100/month minimum).
#   1. Sign up: https://developer.twitter.com/en/portal
#   2. Create app with "Read" permissions
#   3. Copy Bearer Token from app dashboard
#   4. Uncomment the implementation block in agents/media/media_scanner.py
# Without this key, X scanning is skipped — RSS + NewsAPI still run.
# ---------------------------------------------------------------------------
X_BEARER_TOKEN=
