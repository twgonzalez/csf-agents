# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.eggs/
.venv/
venv/
env/

# Environment variables — NEVER commit API keys
.env
.env.*
*.env
!.env.example

# macOS
.DS_Store
.AppleDouble
.LSOverride

# IDEs
.vscode/
.idea/
*.sublime-project
*.sublime-workspace

# Logs (keep directory, not log files)
logs/*.log
logs/*.log.*

# Temporary files
*.tmp
*.temp

# Local email preview files (generated by --preview, not needed in repo)
outputs/email_preview_*.html
outputs/weekly_reports/email_preview_*.html

# LegiScan dataset ZIPs — large binary files, re-downloaded automatically
data/legiscan/CA_*.zip

# Keep empty directories tracked
!data/bills/.gitkeep
!outputs/weekly_reports/.gitkeep
!logs/.gitkeep
!data/legiscan/.gitkeep
